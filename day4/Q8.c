#include <stdio.h>

int main(){
            int a,b;
            printf("Enter The Number\n");
            scanf("%d",&b);

            a=(b*(b+1))/2;

            printf("Sum Obtained of first N Natural Numbers : %d\n", a,b );

            return 0;
}