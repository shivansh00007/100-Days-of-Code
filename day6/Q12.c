#include <stdio.h>

int main(){
            int b;
            printf("Enter The Number");
            scanf("%d",&b);

            if(b<=0){
                        printf("%d is Negative",b);
     
            }
            else{
                        printf("%d is Positive",b);
            }
            return 0;
}